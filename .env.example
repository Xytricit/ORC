# ORC Environment Configuration Example
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED: Flask Secret Key (PRODUCTION)
# =============================================================================
# Generate a secure key with: python -c "import secrets; print(secrets.token_hex(32))"
ORC_SECRET_KEY=your-secret-key-here-change-this-in-production

# =============================================================================
# AI PROVIDER API KEYS (Optional - Only set what you use)
# =============================================================================
# Get free API keys from respective providers
# ORC supports multiple AI providers - you only need to configure the ones you want to use

# Groq (Free tier available) - https://console.groq.com
GROQ_API_KEY=your-groq-api-key-here

# Google Gemini (Free tier available) - https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# OpenAI (Paid) - https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic Claude (Paid) - https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# DeepSeek (Uses OpenAI-compatible endpoint)
DEEPSEEK_API_KEY=your-deepseek-api-key-here

# =============================================================================
# OLLAMA CONFIGURATION (Local AI - No API key needed)
# =============================================================================
# Ollama runs locally on your machine - https://ollama.ai
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama2

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# SQLite database path for web dashboard
DATABASE_URL=sqlite:///instance/orc_web.db

# For PostgreSQL (optional, production):
# DATABASE_URL=postgresql://user:password@localhost:5432/orc_db

# =============================================================================
# WEB SERVER CONFIGURATION
# =============================================================================
# Flask environment (development or production)
FLASK_ENV=development
FLASK_DEBUG=false

# Web server host and port
ORC_HOST=127.0.0.1
ORC_PORT=5000

# =============================================================================
# ORC APPLICATION SETTINGS
# =============================================================================
# Default AI provider to use (auto, ollama, groq, gemini, openai, anthropic, deepseek)
ORC_AI_PROVIDER=auto

# Default AI model (leave empty for provider defaults)
ORC_AI_MODEL=

# Enable debug logging
ORC_DEBUG=false

# Maximum tokens for AI context
ORC_MAX_TOKENS=4000

# Cache directory for ORC data
ORC_CACHE_DIR=.orc

# =============================================================================
# SECURITY SETTINGS
# =============================================================================
# Session cookie security (set to true in production with HTTPS)
SESSION_COOKIE_SECURE=false
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax

# CSRF protection
WTF_CSRF_ENABLED=true

# =============================================================================
# LOGGING
# =============================================================================
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log file path (leave empty for console only)
LOG_FILE=

# =============================================================================
# RATE LIMITING (For API endpoints)
# =============================================================================
# Rate limit for API requests
RATELIMIT_ENABLED=true
RATELIMIT_DEFAULT=100 per hour

# =============================================================================
# CI/CD SECRETS (GitHub Actions, etc.)
# =============================================================================
# These should be set in your CI/CD platform, not in .env files
# CODECOV_TOKEN=your-codecov-token
# DOCKER_USERNAME=your-docker-username
# DOCKER_PASSWORD=your-docker-password
# PYPI_TOKEN=your-pypi-token

# =============================================================================
# NOTES
# =============================================================================
# - Never commit .env files to version control
# - Keep .env.example updated when adding new variables
# - Use different .env files for development/staging/production
# - Store production secrets in secure vaults (AWS Secrets Manager, etc.)
